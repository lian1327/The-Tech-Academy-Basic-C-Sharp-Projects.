using System;

namespace Assignment12
{
    // Define a class named MyClass
    public class MyClass
    {
        // Define a method that takes two integers as parameters, with the second parameter being optional
        public int AddNumbers(int a, int b = 0)
        {
            // Perform a math operation (addition) and return the result
            return a + b;
        }
    }
}
